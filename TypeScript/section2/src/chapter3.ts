//object
let user : {
    id: number, name: string
} = {
    id: 1,
    name : "이인태"
};


let config : {
    readonly apiKey: string,
} = {
    apiKey: "MY API KEY",
}

// 상수를 의미한다.
//config.apiKey = "hacked";